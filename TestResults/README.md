# Validation records

The current September 12, 2026 executions are in `Production`: 108 EditMode tests, 88 PlayMode tests and a later 46-case production subset. The subset repeats PlayMode cases; it is not an additional set of unique tests.

`Production/Visuals`, `MenuUI`, `DemoLabs`, and the current `Unified` PNGs contain actual Unity camera captures. `Unified` holds the latest scene and Android package checks. See `../docs/VLAB_TEST_MATRIX.md` for status, timestamps and physical-device limits.

`Historical/2026-09-10` preserves the prior integration XML and APK metadata. These are historical records, not proof of the current build. The original integration report is `../docs/Integration/validation.md`.
